services:
  databricks-dev:
    build: .
    container_name: databricks-dev
    working_dir: /app
    volumes:
      - .:/app
      - ./.databrickscfg:/root/.databrickscfg:ro
    env_file:
      - .env
    tty: true
    stdin_open: true